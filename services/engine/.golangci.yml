version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused

issues:
  exclude-dirs:
    - pkg/gen
    - third_party
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
